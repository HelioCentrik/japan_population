// CSS zoom removed — fluid flex layout fills the viewport without scaling.
// Both zoom-induced bugs (map zoom inflation, Plotly hover offset) are fixed
// by making .dashboard-outer fluid. See map_resize.js for ResizeObserver that
// handles side-panel open/close without relying on a window resize event.
(function () {})();
